# ====================================
# Basic Room with Overlays
# ====================================
room bathroom {
    name "Bathroom"
    desc "A small private bathroom."
    visited true
    exit out -> bedroom

    overlay if flag set foo {
        text "bar"
    }

    overlay if (flag unset foo, flag complete bar) {
        text "baz"
    }
}
